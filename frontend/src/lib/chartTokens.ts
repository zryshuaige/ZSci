/** ECharts palette tokens — mirrors the 「精密仪器」CSS variables so charts
 *  read as the same system as the DOM around them. Series order follows
 *  statusMeta.TONE_HEX (primary signal blue first). */
export const CHART_SERIES = ["#2563EB", "#059669", "#D97706", "#DC2626", "#7C3AED", "#64748B"];
export const CHART_AXIS = "#606A77"; // = --muted-foreground
export const CHART_GRID = "#E2E6EB"; // = --border
